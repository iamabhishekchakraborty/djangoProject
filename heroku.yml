build:
  docker:
    web: Dockerfile
# define which commands to run for the web and worker processes
run:
  web: gunicorn djangoproject.wsgi:application --bind 0.0.0.0:$PORT
# define tasks that you'd like to execute during a release
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
